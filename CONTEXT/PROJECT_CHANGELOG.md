# PROJECT CHANGELOG

---

## 19.06.2026 — Выбран GitHub Actions автодеплой (настройка в процессе)

**Что решили:**
- Выбран **Вариант 2 — GitHub Actions + автодеплой** для устранения ручного деплоя через Portainer.
- После настройки: `git push` → GitHub Actions → SSH на VPS → `git pull` + `supervisorctl restart telegrambot`.
- Никаких ручных команд в Portainer.

**Где остановились:**
- Нужно зайти на хост-Ubuntu VPS (не в контейнер!) и сгенерировать SSH-ключ.
- Команды: `ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions -N ""`
- Затем добавить приватный ключ в GitHub Secrets и создать `.github/workflows/deploy.yml`.

**Файлы затронуты:** пока только контекст (checkpoint, changelog).

**Откат:** не требуется, настройка ещё не начата.

---

## 19.06.2026 — Смена основной модели: GPT-5 mini вместо Gemini 2.0 Flash

**Что изменили:**
- Основная модель бота изменена с Gemini 2.0 Flash на **GPT-5 mini** (`bot/services/ai.py`)
- Новый порядок fallback: GPT-5 mini → Groq llama-3.1-8b-instant → Gemini 2.0 Flash → GPT-4.1
- Gemini переведён в статус «третий резерв» (остаётся на случай недоступности OpenAI/Groq)
- Также добавлен фикс авто-обрезки истории при ошибке Gemini `too many images and documents`

**Почему:** Gemini 2.0 Flash давал слабые ответы по сравнению с GPT-5 mini; плюс периодически падал с ошибкой превышения лимита документов. GPT-5 mini платный (~$1-15/мес при реальном использовании), но качество значительно выше.

**Файлы затронуты:**
- Изменён: `bot/services/ai.py`

**Откат:** поменять порядок вызовов в `ask_ai()` — вернуть Gemini первым.

---



**Что изменили:**

1. **Google Sheets интеграция** (`bot/services/sheets.py`):
   - Лист «Кредиты и долги» — все кредиты + личные долги перед людьми
   - Лист «История» — каждый платёж с датой/временем
   - Профессиональное форматирование (синяя шапка, цветные строки по критичности)
   - Автоматическое обновление остатков при погашении

2. **AI модель обновлена** (`bot/services/ai.py`):
   - Основная: **Gemini 2.0 Flash** (бесплатный, 1M токенов контекста)
   - Запасная: **Groq llama-3.1-8b-instant**
   - Крайний запас: **GPT-4.1**

3. **Финансовый парсер** (`bot/services/finance.py`):
   - Новый тип `debt_repay` — погашение долга перед людьми
   - Новый тип `query_debts` — показывает кому должен и кто должен
   - Полная функция `get_debts_summary()` с правильными данными
   - Модель парсера: GPT-4.1

4. **Промпт Сенсея** — добавлены полные финансовые данные из письма Бекзода:
   - Все 6 кредитов с деталями (суммы, ставки, даты, просрочки)
   - Личные долги (кому должен и кто должен)
   - Текущая финансовая ситуация

5. **Smart input** (`bot/services/smart_input.py`):
   - Модель: gpt-4o-mini (точнее распознаёт намерения)
   - Финансовые запросы не путаются с задачами

6. **Проактивность** (`bot/services/proactive.py`):
   - Ежедневно в 10:00 напоминает о платежах ближайших 3 дней
   - Обновляет просрочки в Google Sheets
   - Отслеживает личные долги (Сирож ака до 10.07.2026)

**Файлы затронуты:**
- Создан: `bot/services/sheets.py`
- Изменён: `bot/services/ai.py`
- Изменён: `bot/services/finance.py`
- Изменён: `bot/services/smart_input.py`
- Изменён: `bot/services/proactive.py`
- Изменён: `bot/main.py`
- Изменён: `requirements.txt`

**Переменные среды добавлены на сервер:**
- `GOOGLE_SHEETS_ID`
- `GOOGLE_CREDENTIALS_PATH`
- `GEMINI_API_KEY`
- `GROQ_API_KEY`

## 02.06.2026 — Модульная архитектура + перенос токенов в .env

**Что изменили:**
- Разбили монолитный `main.py` (~450 строк) на модульную структуру в папке `bot/`
  (handlers / services / database / utils).
- Перенесли секретные токены (TELEGRAM_TOKEN, OPENAI_API_KEY) из кода в файл `.env`.
- Добавили чтение `.env` через библиотеку `python-dotenv`.
- Данные (база, документы, голос, бэкапы) теперь хранятся в папке `data/`.
- Исправили скрытый баг: команда `/tasks` раньше ошибочно работала как `/task`
  (создавала задачу вместо показа списка). Теперь команды распознаются точно.
- Старый `main.py` стал коротким «переходником» на `bot/main.py`
  (запуск через Supervisor не меняется).

**Почему изменили:**
- Безопасность: токены больше не лежат открыто в коде.
- Масштабируемость: новые функции можно добавлять отдельными модулями,
  не трогая работающий код.
- Стабильность: вся логика сохранена «один в один».

**Какие файлы затронуты:**
- Изменён: `main.py` (теперь переходник)
- Изменён: `requirements.txt` (добавлен `python-dotenv`)
- Созданы: `.env`, `.env.example`, `.gitignore`, `STRUCTURE.md`, `MIGRATION_CHECKLIST.md`
- Созданы: `bot/` со всеми модулями (config, main, handlers/, services/, database/, utils/)
- Созданы папки: `data/` (memory.db, backups, docs, voice)
- Резервная копия: `backups/main.py.bak` (старый рабочий main.py)

**Что проверить:**
- См. `MIGRATION_CHECKLIST.md`, ЧАСТЬ 6 — чек-лист проверки в Telegram.
- Главное: бот отвечает, работают `/task`, `/tasks`, `/remind`, голос, напоминания.

**Откат:**
- `cp /app/main.py.bak /app/main.py` + перезапуск контейнера.
- Подробности отката: `MIGRATION_CHECKLIST.md`, ЧАСТЬ 7.

**ВАЖНО (сделать вручную):**
- Отозвать старые токены и вписать новые в `.env`
  (инструкция: `MIGRATION_CHECKLIST.md`, ЧАСТЬ 0).

---

## 03.06.2026 — Развёртывание новой версии на сервере (VPS) ✅ УСПЕШНО

**Что сделали:**
- Перенесли новую модульную версию кода в контейнер `simplerllm` на VPS (Oracle Cloud).
- Код передавали через консоль Portainer в виде сжатого архива (base64),
  по частям, с проверкой контрольной суммы MD5 (`788dce396d4e72a72d72b6ada6f56954`).
- Распаковали архив в `/app` — папка `bot/` со всеми модулями развернулась.
- Создали на сервере файл `/app/.env` с токенами и параметром `SENSEI_DATA_DIR=/app`.
- Параметр `SENSEI_DATA_DIR=/app` оставляет рабочую базу `memory.db` на старом месте
  (`/app/memory.db`) — данные НЕ переносились и НЕ потеряны.
- Переписали `/etc/supervisord.conf`: добавили секции `[unix_http_server]`,
  `[supervisorctl]`, `[rpcinterface]`, параметры `directory=/app`, `startretries=999`,
  `startsecs=5` — чтобы Supervisor стабильно держал бота и не «сдавался» после сбоев.
- Старый конфиг сохранён: `/etc/supervisord.conf.bak`.

**Проблемы по пути и решения:**
- Веб-консоль Portainer обрезала/искажала длинные вставки → перешли на мелкие блоки
  по 15 строк + проверка MD5; битые строки находили делением пополам и заменяли точечно.
- Команды `supervisorctl` не работали (в конфиге не было нужных секций) → добавили их.
- Бот падал с `ConnectionError: Connection aborted` → причина: несколько копий бота
  с одним токеном одновременно конфликтовали за Telegram. После запуска ОДНОЙ копии
  через Supervisor — связь стабильна.

**Проверки (пройдены):**
- `curl .../getMe` → `"ok":true`, бот `Bekhzod_2_bot` (id 8459988161) — токен живой.
- Код: `CONFIG_OK` + `IMPORTS_OK` — модули исправны, токены читаются.
- Лог: `AI OS STARTED`.
- Бот отвечает в Telegram. ✅

**Текущее состояние на сервере:**
- `/app/bot/` — новый модульный код
- `/app/main.py` — переходник
- `/app/.env` — токены + SENSEI_DATA_DIR=/app
- `/app/memory.db` — рабочая база (на старом месте, данные целы)
- `/app/main.py.bak`, `/app/memory.db.bak` — резервные копии для отката
- `/etc/supervisord.conf` — обновлённый автозапуск (бэкап: .bak)

**Откат (если понадобится):**
- `cp /app/main.py.bak /app/main.py` + `cp /etc/supervisord.conf.bak /etc/supervisord.conf`
  + перезапуск контейнера.

**Известные мелочи (не критично):**
- Предупреждение `Couldn't find ffmpeg` — касается только голосовых сообщений.
  Текст/память/задачи/напоминания работают. Голос проверим и при необходимости
  доустановим ffmpeg отдельным шагом.

---

## 04.06.2026 — Система сохранения контекста (Steering + Spec + Checkpoint)

**Что сделали:**
- Создали папку `.kiro/steering/` с четырьмя файлами авто-контекста (подгружаются
  в КАЖДОМ новом чате, чтобы не терять «понимание проекта и владельца»):
  - `product.md` — что за продукт, кто владелец, 4 сферы, тон, деликатный контекст.
  - `tech.md` — стек, сервер, архитектура кода, ключевые технические факты.
  - `structure.md` — навигация по папкам проекта, порядок чтения, правило чекпоинта.
  - `workflow.md` — правила общения, принципы разработки, ОБЯЗАТЕЛЬНЫЙ чекпоинт.
- Скопировали активный спек `sensei-architecture-env-tokens` (requirements/design/
  tasks/.config.kiro) в `.kiro/specs/` верхнего уровня — зеркало, чтобы контекст
  сохранялся даже при открытии всей папки `SENSEI_PROJECT`, а не только `CODE`.
- Создали `CONTEXT/CHECKPOINT_LATEST.md` — «точка возврата»: кто ты, где остановились,
  следующий шаг, как продолжить после перезагрузки.

**Почему изменили:**
- Владелец переносит проект на Google Диск и хочет, чтобы контекст (включая историю
  чата и «понимание пользователя») не терялся между сессиями и перезагрузками.
- Цель: открыть папку после перезагрузки → ИИ сразу «вспоминает» проект и продолжает.

**Какие файлы затронуты:**
- Созданы: `.kiro/steering/product.md`, `tech.md`, `structure.md`, `workflow.md`
- Созданы (зеркало спека): `.kiro/specs/sensei-architecture-env-tokens/*`
- Создан: `CONTEXT/CHECKPOINT_LATEST.md`
- Обновлены: `CONTEXT/SENSEI_MASTER_CONTEXT_v1.md`, `CONTEXT/START_HERE.md`

**Правило на будущее (введено):**
- После КАЖДОГО значимого изменения обновлять changelog + master context + roadmap
  (если менялся план) + синхронизировать спеки + при необходимости steering.
  Подробности: `.kiro/steering/workflow.md`.

**Откат:**
- Изменения только в документации/контексте (код бота не тронут). Откат не требуется;
  при желании можно удалить созданные файлы — на работу бота это не влияет.

**Следующий шаг:** ЭТАП 1 ROADMAP — защита памяти (копия `memory.db` в приватный TG-канал).

---

## 04.06.2026 — Этап 1: Защита памяти (копии в Telegram-канал) + живой тест

**Что сделали (код, новый модуль, ядро не тронуто):**
- Создан `bot/services/telegram_backup.py` — отправка в приватный канал:
  - база `memory.db` + текстовый экспорт памяти — каждые 6 часов;
  - архив кода бота (`bot/`, без `.env`/БД/`data/`) — раз в неделю;
  - копия по запросу — команда `/backup` и фраза «сделай копию сейчас»;
  - расписание переживает перезапуски (время последней отправки в `data/backup_state.json`);
  - все ошибки логируются и НЕ роняют бота; секреты не логируются.
- Создан `bot/utils/export.py` — Markdown-экспорт памяти (memory + tasks + reminders),
  forward-compatible (работает только по доступным таблицам).
- `bot/config.py` — добавлены `BACKUP_ENABLED`, `BACKUP_DB_INTERVAL_SECONDS` (6 ч),
  `BACKUP_CODE_INTERVAL_SECONDS` (7 дней), `BACKUP_STATE_PATH`, `TELEGRAM_FILE_LIMIT_BYTES`,
  helper `backup_to_channel_enabled()`.
- `bot/handlers/commands.py` — команда `/backup` (точное сопоставление, не ломает /task/tasks/remind).
- `bot/handlers/text.py` — распознавание фразы «сделай копию сейчас».
- `bot/main.py` — запуск `backup_offsite_loop(bot)` как третьего демон-потока,
  дополнительно к reminder_loop и локальному backup_loop (которые не изменены).
- `.env.example` — задокументированы новые настройки.

**Живой тест (выполнен с локальной машины через Telegram Bot API):**
- Проверка: `getMe` → бот `Bekhzod_2_bot`; `getChat` → канал «AI Backups (Bekhzod 2.0)» виден. ✅
- Отправлены в канал и доставлены (RESULT=OK):
  1) архив кода бота (~29 КБ, без секретов);
  2) полный контекст проекта (CONTEXT/DOCS/VPS/.kiro, ~129 КБ);
  3) текстовый экспорт памяти (демо формата).
- Вывод: доставка файлов в приватный канал РАБОТАЕТ.

**Важно (статус развёртывания):**
- Код Этапа 1 готов и проверен ЛОКАЛЬНО (диагностика чистая). На СЕРВЕРЕ пока
  работает версия без этого модуля — автоматические копии каждые 6 ч / неделю
  начнут идти ПОСЛЕ деплоя нового кода в контейнер `simplerllm`.
- Рабочая база `memory.db` живёт на сервере (`/app/memory.db`), поэтому реальные
  копии базы пойдут именно с сервера после деплоя.

**Какие файлы затронуты:**
- Созданы: `bot/services/telegram_backup.py`, `bot/utils/export.py`
- Изменены: `bot/config.py`, `bot/main.py`, `bot/handlers/commands.py`, `bot/handlers/text.py`, `.env.example`
- Спек: `.kiro/specs/memory-backup-telegram/` (requirements готовы; design/tasks — следующий шаг)

**Следующий шаг:** деплой нового кода на VPS (по MIGRATION_CHECKLIST, через Portainer),
проверка `/backup` в Telegram и автоматических копий. Затем оформить design/tasks спека.

**Откат:** новый модуль изолирован; чтобы отключить отправку — `BACKUP_ENABLED=false`
в `.env`. Код можно вернуть из `backups/main.py.bak` (полный откат архитектуры).

---

## 05.06.2026 — Проект на GitHub + автосинхронизация трёх мест

**Что сделали:**
- Залили весь проект `SENSEI_PROJECT` в приватный репозиторий GitHub:
  `github.com/pmuniversal/SENSEI_PROJECT` (через GitHub Desktop, git встроен в него —
  отдельно git в системе НЕ установлен).
- Создали корневой `.gitignore` (`SENSEI_PROJECT/.gitignore`): из GitHub исключены
  секреты и личные данные — `.env`, `CODE/.env`, `**/.env`, `CODE/data/`, `**/data/`,
  `*.bak`, `__pycache__/`, системный мусор. Проверено: в репозитории секретов НЕТ,
  лежит только `CODE/.env.example` (безопасный шаблон).
- Создан `СОХРАНИТЬ.bat` в корне проекта — кнопка «сохранить во все три места»:
  сам находит git (системный или внутри GitHub Desktop), делает `add -A` + `commit` + `push`.
  Поддерживает режим `auto` (без pause/timeout) для запуска из хука.
- Создан Kiro-хук `Синхронизировать контекст` (`Sensei/.kiro/hooks/sensei-sync-context.kiro.hook`,
  событие `agentStop`) — автоматически коммитит и пушит в GitHub после завершения работы агента.
- Создан `CONTEXT/КАК_СИНХРОНИЗИРОВАТЬ.md` — памятка с тремя способами синхронизации.
- Переписан `CONTEXT/START_HERE.md` — теперь это пошаговая точка входа для ЛЮБОГО
  ИИ-агента (порядок чтения контекста, правила работы, обязательный чекпоинт, 3 места хранения).

**Три места хранения (синхронны):**
1. Ноут — рабочая папка `G:\Мой диск\KIRO_PROJECTS\Sensei\SENSEI_PROJECT`.
2. Google Диск — синхронизируется сам (папка внутри `G:\Мой диск\...`).
3. GitHub (приватный) — через `СОХРАНИТЬ.bat` / хук / команду «сделай чекпоинт».

**Почему изменили:**
- Доступ к файлам онлайн с любого устройства + третья независимая копия (GitHub)
  в дополнение к Google Диску. Контекст не теряется при смене ИИ/компьютера.

**Какие файлы затронуты:**
- Созданы: `SENSEI_PROJECT/.gitignore`, `СОХРАНИТЬ.bat`,
  `CONTEXT/КАК_СИНХРОНИЗИРОВАТЬ.md`, `.kiro/hooks/sensei-sync-context.kiro.hook`
- Перезаписан: `CONTEXT/START_HERE.md`
- Код бота НЕ тронут.

**Как пользоваться:** сказать «сделай чекпоинт» → обновляю контекст-файлы и пушу в GitHub.
Либо двойной клик по `СОХРАНИТЬ.bat`. Google Диск — сам.

**Откат:** только документация/инфраструктура, код бота не затронут. При желании можно
удалить bat/хук — на работу бота не влияет.

**Следующий шаг (без изменений):** ЭТАП 1 ROADMAP — деплой кода защиты памяти на VPS
(контейнер `simplerllm`), проверка `/backup` и автокопий каждые 6 часов.

---

## 05.06.2026 — Чекпоинт перед сменой ноутбука (хэндовер)

**Что сделали:**
- Зафиксировали контекст перед переходом владельца на другой ноутбук.
- Подтверждена автосинхронизация трёх мест (ноут + Google Диск + GitHub),
  настроенная в параллельном чате: `СОХРАНИТЬ.bat`, хук `agentStop`, корневой `.gitignore`.
- Репозиторий `github.com/pmuniversal/SENSEI_PROJECT`, ветка `main` — синхронна с origin.
- Обновлён `CONTEXT/CHECKPOINT_LATEST.md`: добавлена памятка по хэндоверу на новый ноут.

**Где остановились (актуально):**
- Этап 1 «Защита памяти» — код готов и проверен живым тестом (доставка в канал работает).
- ⏳ Следующий шаг: ДЕПЛОЙ кода Этапа 1 на VPS (контейнер `simplerllm`) по
  `CODE/MIGRATION_CHECKLIST.md`, проверка `/backup` и автокопий каждые 6 часов.

**Файлы:** обновлены `CONTEXT/CHECKPOINT_LATEST.md`, `CONTEXT/PROJECT_CHANGELOG.md`.
**Откат:** не требуется (только контекст-документация).

---

## 12.06.2026 — Внедрены переносимые правила ассистента + реестр функций

**Что сделали:**
- Прочитан `ASSISTANT_RULES_PORTABLE.md`; новые правила перенесены в steering:
  - `.kiro/steering/autonomy.md` — автономия и доверенные действия (внутри проекта —
    сам; система ПК / вне проекта — спрашивать).
  - `.kiro/steering/principles.md` — «всё гениальное просто», измеримость до/после,
    контроль результата, Champion/Challenger, конфигурируемые параметры,
    документы ВСЕГДА от лица пользователя (никогда не помечать как сделанное ИИ),
    протокол измеримости функций.
- Создан `CONTEXT/FEATURE_REGISTRY.md` — реестр функций с метрикой «до/после»,
  заполнен по уже сделанному (архитектура, контекст-система, синхронизация, Этап 1).
- Обновлён `.kiro/steering/workflow.md`: добавлен шаг обновления реестра функций и
  цикл синка с `git pull --rebase --autostash` (безопасно при работе с нескольких ПК).

**Почему:** единый, переносимый «характер» ассистента; измеримость функций;
безопасная синхронизация при работе с разных ноутбуков.

**Файлы:** созданы `autonomy.md`, `principles.md`, `FEATURE_REGISTRY.md`;
обновлены `workflow.md`, `PROJECT_CHANGELOG.md`.
**Откат:** только документация/steering; на код бота не влияет.

---

## 12.06.2026 — Трёхуровневая иерархия + УП (_PM) + первая синхронизация правил

**Что сделали:**
- Создана папка `KIRO_PROJECTS/_PM` (Управляющий проектами, уровень 1):
  - `.kiro/steering/pm-role.md` — роль, иерархия, обязанности, что можно/нельзя, типовые команды.
  - `.kiro/steering/` + `RULES/` — переносимые правила (autonomy, principles, working-rules).
  - `RULES_CHANGELOG.md` — журнал изменений эталона.
  - `SYNC_RULES.md` — инструкция синхронизации (3 способа).
  - `sync.ps1` — скрипт авто-синхронизации в все/выбранные проекты.
- Выполнена первая синхронизация: autonomy/principles/working-rules из `_PM/RULES/`
  скопированы в .kiro/steering/ всех 5 проектов (Pulinform, Sensei, YouTube_US,
  SaaS_UZ_Stores, SaaS_Local_Services). Все OK.

**Иерархия (зафиксирована):**
  Уровень 1: УП (_PM) — управляет правилами и поведением агентов.
  Уровень 2: Сенсей-бот (Telegram) — читает статусы, информирует владельца.
  Уровень 3: Агенты проектов — ведут каждый свой проект в отдельном окне Kiro.

**УП ЗАПРЕЩЕНО:** менять контент/код/roadmap проектов. Только steering.

**Файлы:** `KIRO_PROJECTS/_PM/` (вне Sensei-git — не в GitHub).
**Откат:** правила в проектах — это копии; можно удалить или перезаписать вручную.

---

## 12.06.2026 — Идеи из сброса разнесены в ROADMAP Sensei (Этапы 15–17)

**Что сделали:**
- Сверили список идей владельца с уже зашитым. Принципы 20/80, углубление, обучение
  других, удержание вектора — УЖЕ в `SENSEI_SYSTEM_PROMPT.md` (не дублировали).
  ICF-коуч — Этап 10; проактивность — Этап 6; голос→текст — Этап 2; документы — Этап 8.
- Новые идеи добавлены в `ROADMAP.md`:
  - Этап 15 — База знаний и «умная память» (книги/Коран/хадисы, NotebookLM-ингест
    YouTube/IG-рилсов, навык структурирования, Obsidian-структура, файл skills по
    направлениям, фильтрация качественной инфо, Kiro-анализ сайта скиллов).
  - Этап 16 — Мультимодальность и управление ПК (картинки, изучение видео; управление
    курсором/клавиатурой/микрофоном — помечено как рискованное).
  - Этап 17 — Кросс-проектная осведомлённость Сенсея (читать PROJECT_STATUS, помогать управлять).
- `BACKLOG.md` — добавлен индекс этих идей со ссылками на этапы.

**Файлы:** `CONTEXT/ROADMAP.md`, `CONTEXT/BACKLOG.md`, `CONTEXT/PROJECT_CHANGELOG.md`.
**Откат:** только документация.

---

## 15.06.2026 — Этап 1 задеплоен и работает ✅

**Что сделали:**
- Задеплоили 6 файлов Этапа 1 на сервер через GitHub (репо временно публичный, потом снова приватный):
  `bot/config.py`, `bot/main.py`, `bot/handlers/text.py`, `bot/handlers/commands.py`,
  `bot/services/telegram_backup.py`, `bot/utils/export.py`.
- Добавили `BACKUP_CHANNEL_ID=-1003978847512` в `/app/.env` на сервере.
- Перезапустили бота (`supervisorctl restart telegrambot`).

**Результат проверки (все пройдено):**
- Лог: `AI OS STARTED` + `Цикл внешних копий запущен`.
- При старте сразу ушли 3 копии в канал «AI Backups»: база memory.db, текстовый экспорт, архив кода.
- `/backup` в Telegram работает: «📤 Отправляю...» → «✅ Копия отправлена».
- Автокопии каждые 6 ч (база + экспорт) и раз в неделю (архив кода) — запущены.

**Этап 1 ЗАКРЫТ.**

**Следующий шаг:** Этап 2 (голос → Word/txt + резюме встреч, установить ffmpeg)
или Этап 15 (база знаний/ингест) — выбор за владельцем.

---

## 15.06.2026 — Этапы 2, 3, 7 задеплоены и работают ✅

**Что сделали за сессию:**
- **Этап 2 (Голос):** новый voice.py + voice_processor.py + meeting.py.
  Транскрипция рус+узб, поддержка voice/audio/m4a/mp3, умные режимы (чат/резюме/
  собрание Bayonnoma-Agenda/финансы), вопрос контекста. ffmpeg установлен.
- **Этап 3 (Умный ввод):** smart_input.py. Бот определяет задачу/напоминание/
  запрос без команд, привязывает к 4 сферам автоматически.
- **Этап 7 (Долгая память):** profile.py. Таблица profile в БД, извлечение фактов
  GPT в фоне после каждого разговора, подгрузка профиля в контекст AI.
- Исправлен API-ключ OpenAI на сервере (401 → OK).
- Установлен ffmpeg на сервере.
- Записан в backlog: файлы >20 МБ (нужен локальный Telegram Bot API Server).

**Деплой:** через GitHub (curl raw files) — репо временно публичный на ~5 мин.

**Файлы на сервере обновлены:**
  bot/handlers/voice.py, bot/handlers/text.py, bot/services/voice_processor.py,
  bot/services/meeting.py, bot/services/smart_input.py, bot/services/profile.py,
  bot/services/ai.py

**Следующий шаг:** Этап 5 (финансист) или Этап 6 (проактивность) или Этап 15 (ингест).

---

## 16.06.2026 — Этапы 6, fix напоминаний, часовой пояс

**Что сделали:**
- Этап 6 (Проактивность): `proactive.py` — утро 9:00, вечер 21:00, фокус через день 14:00, дедлайны 8:00. `OWNER_CHAT_ID=529561930` добавлен в `.env` на сервере.
- Fix напоминаний: нормализация форматов времени, команды `/reminders` и `/delreminder`.
- Fix часового пояса: сервер был в UTC, добавлен `TZ=Asia/Tashkent` → напоминания теперь работают по ташкентскому времени (+5).
- Системный промпт обновлён полностью (4 сферы, ICF, финансы, ислам, структурирование).
- Репо приватным после каждого деплоя.
