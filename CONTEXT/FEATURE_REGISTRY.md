# 📊 FEATURE REGISTRY — реестр функций (измеримость до/после)

> Каждая значимая функция фиксируется здесь с метрикой «до/после».
> Правило: уточнять у владельца суть, не додумывать; обновлять на чекпоинте;
> каждое изменение — новая строка с датой. Подробнее: `.kiro/steering/principles.md`.

---

## Функция: Модульная архитектура + токены в .env

- **Что реализовано:** монолитный `main.py` разбит на пакет `bot/`; токены вынесены в `.env`.
- **Как реализовано:** слои handlers/services/database/utils; `config.py` читает `.env` (python-dotenv); shim `/app/main.py`.
- **Метрика ДО:** 1 файл ~450 строк; токены захардкожены в коде (риск утечки); добавление функции = правка монолита.
- **Метрика ПОСЛЕ:** ~15 модулей; 0 секретов в коде; новая функция = новый файл без правки ядра. Баг `/tasks` устранён.
- **Дата:** 02–03.06.2026 · **Статус:** внедрено (развёрнуто на VPS).
- **Как показать результат:** бот отвечает, лог `AI OS STARTED`; `/tasks` показывает список, а не создаёт задачу.

## Функция: Система сохранения контекста (steering + зеркало + чекпоинты)

- **Что реализовано:** авто-контекст в `.kiro/steering/`, зеркало спеков, `CHECKPOINT_LATEST.md`.
- **Как реализовано:** steering-файлы (always), правило чекпоинта, дублирование спеков на верхний уровень.
- **Метрика ДО:** новый чат/ноут = пересказывать контекст вручную; риск потери прогресса.
- **Метрика ПОСЛЕ:** контекст подхватывается автоматически в каждом чате; 0 потерь при смене ноута.
- **Дата:** 04.06.2026 · **Статус:** внедрено.
- **Как показать результат:** в новом чате ассистент сразу «узнаёт» проект и владельца.

## Функция: Автосинхронизация 3 мест (ноут + Google Диск + GitHub)

- **Что реализовано:** `СОХРАНИТЬ.bat`, хук `agentStop`, корневой `.gitignore`.
- **Как реализовано:** git add/commit/push; секреты (`.env`, `data/`) исключены из GitHub.
- **Метрика ДО:** файлы в 1 месте; нет версий/истории/бэкапа кода вне ноута.
- **Метрика ПОСЛЕ:** 3 синхронных копии; полная история коммитов; откат возможен.
- **Дата:** 05.06.2026 · **Статус:** внедрено.
- **Как показать результат:** `git log` на GitHub; файлы открываются с любого ПК.

## Функция: Этап 1 — Защита памяти (копии в Telegram-канал)

- **Что реализовано:** база+экспорт каждые 6 ч, архив кода раз в неделю, команда `/backup` и фраза «сделай копию сейчас».
- **Как реализовано:** модуль `bot/services/telegram_backup.py` + `bot/utils/export.py`; фоновый поток; расписание в `data/backup_state.json`.
- **Метрика ДО:** копии `memory.db` только на том же сервере → гибель сервера = потеря всего.
- **Метрика ПОСЛЕ:** внешние копии вне сервера каждые 6 ч; восстановление памяти и кода возможно после гибели VPS.
- **Дата:** 04.06.2026 · **Статус:** код готов, живой тест пройден; ⏳ ожидает деплоя на VPS.
- **Как показать результат:** файлы в канале «AI Backups»; после деплоя — свежие копии каждые 6 часов.
