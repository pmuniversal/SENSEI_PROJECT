# 📍 CHECKPOINT — ПОСЛЕДНЯЯ ТОЧКА СОХРАНЕНИЯ

> Это «точка возврата» проекта. Открой этот файл первым после перезагрузки —
> здесь коротко: кто ты, где мы остановились и что делать дальше.
> Дата снимка: **05.06.2026**

> ⚠️ ХЭНДОВЕР: владелец продолжит работу **с ДРУГОГО ноутбука**. Файлы синхронны
> в 3 местах (ноут + Google Диск + GitHub `github.com/pmuniversal/SENSEI_PROJECT`).
> На новом ноуте: открой папку `SENSEI_PROJECT` из Google Диска (или склонируй из
> GitHub) — весь контекст и код уже здесь. Секреты (`.env`) в GitHub НЕ хранятся,
> поэтому при клоне из GitHub нужно будет вписать токены в `CODE/.env` заново
> (на машине с синхронизированным Google Диском `.env` уже на месте).

---

## КТО ТЫ (для ИИ — быстрое узнавание)

- Владелец — **Бекзод**, проект **Sensei** (личный AI-наставник в Telegram).
- Бекзод **не программист** → всё простым языком, пошагово, на русском, по одной команде.
- Тон Сенсея к пользователю: жёсткий наставник-стратег, без лести, но не унижает.
- Деликатно: у владельца тяжёлый период — снижать перегруз, дробить задачи, поддерживать.
- 4 сферы: **Pulinform** (работа), **Американский YouTube**, **Продажа сайтов в США**, **Жизнь/быт**.

Полное «узнавание» хранится в `.kiro/steering/` (product.md, tech.md, structure.md, workflow.md)
— эти файлы ИИ подгружает автоматически.

---

## ГДЕ МЫ ОСТАНОВИЛИСЬ (статус на 04.06.2026)

✅ **Фундамент готов и работает на сервере.**
- Монолитный `main.py` разбит на модульную структуру `bot/` (handlers/services/database/utils).
- Токены вынесены в `.env` (python-dotenv).
- Данные — через `SENSEI_DATA_DIR=/app` база осталась в `/app/memory.db` (НЕ потеряна).
- Исправлен баг `/tasks`.
- Новая версия развёрнута в контейнере `simplerllm` (Oracle Cloud), бот отвечает, лог `AI OS STARTED`.
- Спек `sensei-architecture-env-tokens` — все 10 задач выполнены.

✅ **Система сохранения контекста настроена (04.06.2026):**
- Созданы steering-файлы в `.kiro/steering/` — авто-контекст для каждого нового чата.
- Спек скопирован в `.kiro/specs/` (зеркало) — на случай открытия папки целиком.
- Введено правило «чекпоинт после каждого изменения» (см. `.kiro/steering/workflow.md`).

🔨 **Этап 1 «Защита памяти» — код готов, живой тест пройден (04.06.2026):**
- Новый модуль `bot/services/telegram_backup.py` + `bot/utils/export.py` (ядро не тронуто).
- Каждые 6 ч: база + текстовый экспорт; раз в неделю: архив кода; команда `/backup` и фраза «сделай копию сейчас».
- Живой тест: код + контекст + экспорт успешно доставлены в канал «AI Backups (Bekhzod 2.0)».
- ⏳ ОСТАЛОСЬ: задеплоить новый код на VPS, чтобы автокопии пошли с сервера.

✅ **Проект на GitHub + автосинхронизация (05.06.2026):**
- Весь проект залит в приватный GitHub: `github.com/pmuniversal/SENSEI_PROJECT`.
- Файлы теперь в 3 синхронных местах: ноут + Google Диск (сам) + GitHub.
- Секреты (`.env`, `data/`) в GitHub НЕ попадают (корневой `.gitignore`). Проверено.
- Кнопка `СОХРАНИТЬ.bat` (в корне) и хук `agentStop` — автокоммит+push в GitHub.
- Команда «сделай чекпоинт» = обновить контекст-файлы + запушить в GitHub.

---

## СЛЕДУЮЩИЙ ШАГ (куда идём)

Задеплоить код Этапа 1 на сервер (контейнер `simplerllm`) по `CODE/MIGRATION_CHECKLIST.md`
через Portainer, проверить `/backup` в Telegram и автоматические копии каждые 6 часов.
Затем оформить design/tasks спека `memory-backup-telegram` и закрыть этап.

Известная мелочь: на сервере предупреждение `Couldn't find ffmpeg` — влияет только на
голосовые; при переходе к Этапу 2 (голос) доустановим ffmpeg.

---

## КАК ПРОДОЛЖИТЬ РАЗГОВОР ПОСЛЕ ПЕРЕЗАГРУЗКИ

1. Открой папку `SENSEI_PROJECT` в Kiro.
2. Steering-файлы подхватятся сами — я снова «вспомню» тебя и проект.
3. Скажи: «Продолжаем» или «Начинаем Этап 1» — и мы пойдём дальше с этого места.

Если что-то не подхватилось — попроси меня прочитать `CONTEXT/CHECKPOINT_LATEST.md`
и `CONTEXT/PROJECT_CHANGELOG.md` (последняя запись = где мы остановились).

---

## КАК СОХРАНЯТЬ ПРОГРЕСС (3 места синхронно)

- Скажи **«сделай чекпоинт»** → я обновлю контекст-файлы и запушу в GitHub.
- Или двойной клик по **`СОХРАНИТЬ.bat`** в корне проекта.
- Google Диск синхронизируется сам. Подробно — `CONTEXT/КАК_СИНХРОНИЗИРОВАТЬ.md`.
- Секреты (`.env`, `data/`) в GitHub НЕ уходят — это правильно.
