"""
main.py — Переходник (shim) для запуска бота Sensei
====================================================

ПРОСТЫМИ СЛОВАМИ:
Раньше весь код бота был прямо в этом файле. Теперь код разбит на модули
в папке bot/, а здесь остался только короткий «переходник»: он запускает
настоящую точку входа bot/main.py.

Зачем так:
Supervisor на сервере уже настроен запускать /app/main.py. Благодаря этому
переходнику нам НЕ нужно ничего менять в настройках сервера —
запуск снаружи выглядит точно так же, как раньше.

Никаких секретных токенов в этом файле больше нет — они в файле .env.
"""

from bot.main import main

if __name__ == "__main__":
    main()
