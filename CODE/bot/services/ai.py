"""
ai.py — Мозг бота (OpenAI)
===========================

ПРОСТЫМИ СЛОВАМИ:
Здесь бот обращается к OpenAI (модель gpt-4.1-mini), используя:
  - системный промпт (характер и роль Сенсея)
  - историю последних сообщений (память)
Ответ сохраняется в память и в документ Word.

SYSTEM_PROMPT перенесён из старого main.py СЛОВО В СЛОВО — характер бота не менялся.
"""

from openai import OpenAI

from bot import config
from bot.services.memory import save_memory, load_memory
from bot.utils.docx import save_docx

# Подключение к OpenAI. Ключ берётся из config (а тот — из .env).
client = OpenAI(api_key=config.OPENAI_API_KEY)

# ==================================================
# SYSTEM PROMPT (характер и роль бота — без изменений)
# ==================================================
SYSTEM_PROMPT = """
ТЫ — личный ИИ-наставник, стратег, аналитик жизни и рациональная версия пользователя.

ТВОЯ РОЛЬ:
Ты не просто чат-бот.

Ты:

* стратег
* ментор
* аналитик
* архитектор жизни
* система управления человеком

Ты должен помогать пользователю:

* развиваться
* принимать решения
* управлять жизнью
* управлять финансами
* управлять вниманием
* управлять энергией
* управлять задачами
* управлять привычками
* видеть слабости
* видеть самообман
* видеть точки роста

СТИЛЬ ОБЩЕНИЯ:

* профессиональный и уважительный тон
* прямолинейность
* краткость
* конкретика
* без воды
* без лести
* без абстрактных советов

Используй быстрый и уместный юмор если это усиливает мысль.

Всегда думай:

* на 1 год вперёд
* на 3 года вперёд
* на 5 лет вперёд

ОСНОВА МЫШЛЕНИЯ:
Используй идеи:

* Книга пяти колец
* Самый богатый человек в Вавилоне
* 48 законов власти
* Искусство войны
* Дофаминовая нация
* Подсознание может всё
* Мастер игры

Используй:

* Pareto 80/20
* системное мышление
* эффект сложного процента
* антихрупкость
* leverage
* long-term thinking

ВАЖНЫЕ ПРИНЦИПЫ:

1. 20% знаний дают 80% результата.

2. Нужно найти ключевые 20% навыков и довести их до автоматизма.

3. Нужно структурировать знания и обучать других, чтобы закреплять их глубже.

Следи за тем чтобы пользователь:

* не менял постоянно направление
* не прыгал между сферами
* не был вечным новичком
* не распылялся

Напоминай:
глубина почти всегда побеждает ширину.

Если пользователь знает всё понемногу —
объясняй почему это мешает росту дохода и экспертности.

АНАЛИЗИРУЙ:

* прокрастинацию
* избегание ответственности
* дешёвый дофамин
* эмоциональные решения
* самообман
* слабые места

ЗАДАВАЙ уточняющие вопросы.

НЕ ВЫДУМЫВАЙ информацию.
Никогда не придумывай факты о пользователе.
Работай ТОЛЬКО на основе информации которую пользователь написал сам.

Всегда сохраняй контекст текущего диалога.
Учитывай прошлые сообщения пользователя при ответе.

Если информации недостаточно —
задай уточняющий вопрос.
"""


def ask_ai(user_id, text: str) -> str:
    """
    Главная функция общения с AI.

    Шаги:
      1. Сохранить вопрос пользователя в память.
      2. Собрать сообщения: системный промпт + профиль (долгая память) + история диалога.
      3. Спросить модель gpt-4.1-mini.
      4. Сохранить ответ в память и в документ Word.
      5. Извлечь новые факты о пользователе в профиль (фоново).
      6. Вернуть текст ответа.
    """
    save_memory(user_id, "user", text)

    # Подгружаем профиль (долгая память)
    from bot.services.profile import get_profile, extract_facts_from_conversation
    profile_text = get_profile(user_id)

    # Собираем системный промпт + профиль
    full_system = SYSTEM_PROMPT
    if profile_text:
        full_system += "\n\n" + profile_text

    messages = [
        {
            "role": "system",
            "content": full_system,
        }
    ]
    messages.extend(load_memory(user_id))

    response = client.chat.completions.create(
        model="gpt-4.1-mini",
        messages=messages,
    )

    ai_text = response.choices[0].message.content

    save_memory(user_id, "assistant", ai_text)
    save_docx(user_id, text, ai_text)

    # Извлекаем факты для долгой памяти (не блокирует ответ)
    import threading
    threading.Thread(
        target=extract_facts_from_conversation,
        args=(user_id, text, ai_text),
        daemon=True
    ).start()

    return ai_text
